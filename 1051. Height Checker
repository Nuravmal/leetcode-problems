
//comparing
class Solution {
public:
    int heightChecker(vector<int>& heights) {
       vector<int>h;
    h=heights;
        int c=0;
        sort(heights.begin(),heights.end());
        for(int i=0;i<heights.size();i++)
        {
            if(h[i]!=heights[i])
                c++;
        }
        return c;
    }};
