class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        map<int,int>m1;
        for(auto i:nums)
        {
            if(m1.count(i))   
                return true;
        else
            m1.insert({i,1});
        }
        
    return false;}
};
