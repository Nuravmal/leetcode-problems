using map
class Solution {
public:
    int numJewelsInStones(string J, string S) {
        map<char,int>m1;
        for(auto c:J)
            m1.insert({c,1});
        int res=0;
        
      for(auto d:S)
     if(m1[d]) res++;
        
        return res;}};
